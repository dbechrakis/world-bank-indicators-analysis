name: Evidence checks
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: '3.12'
      - run: python -m pip install pandas pycountry pycountry-convert
      - name: Validate committed evidence
        run: python ci/verify_evidence.py
      - name: Run regression tests
        run: python -m unittest discover -s tests -v
      - name: Rebuild SQL database from committed data
        run: python sql/build_database.py
